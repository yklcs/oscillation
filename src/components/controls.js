import React from "react"

import { card, controls } from "../styles/layout.module.css"

const Controls = () => (
  <div className={`${card} ${controls}`}>
    Test
  </div>
)

export default Controls